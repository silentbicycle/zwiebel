A simple work/break interval timer.

To use, `(require 'zwiebel)`. You'll probably want to assign a
keybinding to `zwiebel-dwim`:

    (global-set-key (kbd "C-c z") 'zwiebel-dwim)

And register callbacks for the various hooks.
